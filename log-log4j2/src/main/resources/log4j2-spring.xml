<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL-->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置文件和重新配置本身，设置间隔秒数-->
<Configuration status="WARN" monitorInterval="30">
    <!--定义变量-->
    <Properties>
        <!--name的值是变量的名称，value的值时变量定义的值。通过定义的值会被插入到logger上下文中。定义变量后，可以使“${}”来使用变量-->
        <property name="fileNamePrefix" value="${sys:user.home}/logs"/>
        <property name="logPattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level --- [%thread] %logger{50} - %msg%n"/>
        <property name="logFileNamePattern" value="%d{yyyy-MM-dd}.%i.log"/>
    </Properties>

    <!--先定义所有的appender-->
    <Appenders>
        <!--文件会输出所有日志，可配置为每次运行程序自动清空，由append属性决定，适合开发和测试阶段用-->
        <File name="DebugLog" fileName="${fileNamePrefix}/dev.log" append="false">
            <PatternLayout pattern="${logPattern}"/>
        </File>

        <!--输出控制台的配置-->
        <Console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout pattern="${logPattern}"/>
        </Console>

        <!--这个会打印出所有的info及以上级别的日志，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <RollingFile name="RollingFileInfo" fileName="${fileNamePrefix}/info.log"
                     filePattern="${fileNamePrefix}/info/${date:yyyy-MM}/info-${logFileNamePattern}}">
            <!--控制只输出level及以上级别的日志（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="INFO" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${logPattern}"/>
            <Policies>
                <!--配置基于时间的滚动策略，interval属性用来指定多久滚动一次，默认是1 hour；modulate属性表示是否调整时间间隔以使在时间间隔边界发生滚动-->
                <!--例如：假设小时为具体的时间单元，当前时间为上午3点，时间间隔为4，第一次发送rollover是在上午4点，接下来是上午8点，接着是中午，接着是下午4点等发生。-->
                <!--<TimeBasedTriggeringPolicy interval="1" modulate="true"/>-->
                <!--配置基于日志文件大小的滚动策略-->
                <SizeBasedTriggeringPolicy size="100M"/>
            </Policies>
            <!--DefaultRolloverStrategy属性如不设置，则默认为最多同一文件夹下7个文件，这里设置了20-->
            <DefaultRolloverStrategy max="20"/>
        </RollingFile>

        <!--这个会打印出所有的warn及以上级别的日志，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <RollingFile name="RollingFileWarn" fileName="${fileNamePrefix}/warn.log"
                     filePattern="${fileNamePrefix}/warn/${date:yyyy-MM}/warn-${logFileNamePattern}">
            <ThresholdFilter level="WARN" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${logPattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100M"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!--这个会打印出所有的error及以上级别的日志，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <RollingFile name="RollingFileError" fileName="${fileNamePrefix}/error.log"
                     filePattern="${fileNamePrefix}/error/${date:yyyy-MM}/error-${logFileNamePattern}">
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="${logPattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100M"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <!--然后定义logger，只有定义了logger并引入的appender，appender才会生效-->
    <Loggers>
        <!--Logger标签单独配置Spring和Mybatis包只输出info及以上级别的日志-->
        <Logger name="org.springframework" level="INFO"/>
        <Logger name="org.mybatis" level="INFO"/>

        <!--Root标签激活上面Appender定义的配置-->
        <Root level="all">
            <AppenderRef ref="DebugLog"/>
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFileInfo"/>
            <AppenderRef ref="RollingFileWarn"/>
            <AppenderRef ref="RollingFileError"/>
        </Root>
    </Loggers>
</Configuration>