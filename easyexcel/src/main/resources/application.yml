# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://IP:3307/easyexcel?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root

  # 文件相关配置
  servlet:
    multipart:
      enabled: true # 开启文件上传
      location: C:\Users\10231\Pictures # 文件上传的路径
      max-request-size: 200MB # 上传的总文件大小
      max-file-size: 50MB # 上传的单个文件大小

